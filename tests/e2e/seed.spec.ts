import { test } from "./fixtures";

// Seed: navigate to /en/jobs. All scenario tests start from this state.
test("seed", async ({ page }) => {
  // fixture already navigated; this body is intentionally empty
  void page;
});
