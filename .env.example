# Server-only — base URL for the koreaJobApiV2 REST API.
# Defaults to the live Coolify deployment when unset.
API_BASE_URL=https://api-srku356jbc5fqtrtwff3j3pd.50.146.245.162.sslip.io

# Public — absolute site URL used for canonical URLs, sitemap, and OG tags.
# In development you can leave this blank; it falls back to http://localhost:3000.
NEXT_PUBLIC_SITE_URL=https://jobs.example.com

# Optional — bump when you want to revalidate the entire job list cache sooner
# than the default 60s. Units: seconds.
# REVALIDATE_JOBS=60
